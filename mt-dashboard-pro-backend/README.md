# Docker ROS2 Node contribution

This project is a template example of a URCap with a Docker ROS2 Node contribution

### Installation
To install the URCap type:

`$ npm install`

### Build
To build the URCap type:

`$ npm run build`

### Deploy
To deploy the URCap to the simulator type:

`$ npm run install-urcap`

## Further help

Get more help from the included SDK documentation.
